---
const empresas = [
  { nombre: 'Gobierno de Chile', subtitulo: 'Red de Municipios' },
  { nombre: 'Corfo', subtitulo: 'Programas Semilla' },
  { nombre: 'Startup Chile', subtitulo: 'Talento Digital' },
  { nombre: 'BancoEstado', subtitulo: 'Iniciativas PyME' },
  { nombre: 'Fundación Chile', subtitulo: 'Empleos Verdes' },
  { nombre: 'Sence', subtitulo: 'Capacitación' }
];
---
<section class="relative overflow-hidden rounded-[40px] border border-white/70 bg-white/85 p-10 shadow-deluxe backdrop-blur">
  <div class="pointer-events-none absolute inset-0 -z-10 bg-gradient-to-br from-primary-50/70 via-white to-secondary-50/60"></div>
  <div class="pointer-events-none absolute inset-x-24 top-1/2 -z-10 h-1/2 -translate-y-1/2 rounded-full bg-aurora-gradient opacity-30 blur-3xl"></div>
  <div class="relative flex flex-col gap-6 text-center">
    <p class="section-title">Confían en nosotros</p>
    <h2 class="font-display text-2xl font-semibold text-slate-900 sm:text-3xl">
      Alianzas público-privadas para acelerar oportunidades
    </h2>
    <p class="mx-auto max-w-2xl text-sm text-slate-600">
      Colaboramos con instituciones líderes para impulsar la empleabilidad, diversidad y reconversión laboral en todo el país.
    </p>
    <div class="grid gap-5 sm:grid-cols-3">
      {empresas.map((empresa) => (
        <div class="group relative flex flex-col items-center gap-3 overflow-hidden rounded-[30px] border border-white/60 bg-white/75 p-6 shadow-innerGlow transition hover:-translate-y-1 hover:shadow-deluxe">
          <div class="pointer-events-none absolute inset-x-0 top-0 h-[1px] bg-gradient-to-r from-transparent via-primary-200 to-transparent"></div>
          <div class="grid h-16 w-16 place-items-center rounded-2xl bg-primary-500/10 text-primary-600 shadow-outline">
            <span class="font-display text-xl">{empresa.nombre.at(0)}</span>
          </div>
          <div class="space-y-1">
            <p class="font-semibold text-slate-900">{empresa.nombre}</p>
            <p class="text-[11px] uppercase tracking-[0.35em] text-slate-400">{empresa.subtitulo}</p>
          </div>
        </div>
      ))}
    </div>
    <p class="text-xs font-semibold uppercase tracking-[0.35em] text-slate-400">
      +35 convenios activos con municipios, universidades y cámaras empresariales
    </p>
  </div>
</section>
