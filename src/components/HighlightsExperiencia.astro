---
import { Sparkles, ShieldCheck, Radar } from 'lucide-astro';

const highlights = [
  {
    titulo: 'Curaduría humana + IA',
    descripcion:
      'Cada vacante pasa por validación editorial y un modelo predictivo que prioriza diversidad, bienestar y competitividad salarial.',
    icono: Sparkles,
    etiqueta: 'Selección premium'
  },
  {
    titulo: 'Monitoreo territorial vivo',
    descripcion:
      'Cruce de datos SENDA, INE y catastros privados para entender movilidad, accesibilidad y brechas por comuna en tiempo real.',
    icono: Radar,
    etiqueta: 'Data geoespacial'
  },
  {
    titulo: 'Compliance listo para auditar',
    descripcion:
      'Reportes descargables con indicadores ESG, equidad de género y huella social para empresas que licitan con el Estado.',
    icono: ShieldCheck,
    etiqueta: 'Confianza institucional'
  }
];
---
<section class="mt-20 space-y-12">
  <div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between">
    <div class="space-y-3">
      <span class="section-title">Experiencia superior</span>
      <h2 class="font-display text-3xl font-semibold text-slate-900 sm:text-4xl">
        Diferenciales que elevan la plataforma a nivel mundial
      </h2>
    </div>
    <p class="max-w-xl text-sm text-slate-600">
      Mezclamos data science, acompañamiento humano y visualización de territorios para ofrecer una experiencia impecable para talento y empleadores.
    </p>
  </div>
  <div class="grid gap-6 md:grid-cols-3">
    {highlights.map((highlight) => (
      <div class="relative overflow-hidden rounded-[32px] border border-white/70 bg-gradient-to-br from-white/95 via-white/70 to-primary-50/70 p-7 shadow-deluxe">
        <div class="pointer-events-none absolute -top-20 right-0 h-48 w-48 rounded-full bg-aurora-gradient opacity-30 blur-3xl"></div>
        <div class="relative flex h-full flex-col gap-5">
          <span class="inline-flex w-fit items-center gap-2 rounded-full border border-white/60 bg-white/60 px-3 py-1 text-[11px] font-semibold uppercase tracking-[0.28em] text-primary-700">
            {highlight.etiqueta}
          </span>
          <div class="flex items-center gap-3">
            <span class="grid h-12 w-12 place-items-center rounded-2xl bg-primary-500/10 text-primary-600 shadow-outline">
              <highlight.icono class="h-5 w-5" />
            </span>
            <h3 class="font-display text-xl font-semibold text-slate-900">{highlight.titulo}</h3>
          </div>
          <p class="text-sm leading-relaxed text-slate-600">{highlight.descripcion}</p>
          <div class="mt-auto flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.3em] text-secondary-600">
            <span class="h-2 w-2 rounded-full bg-secondary-400"></span>
            Experiencia certificable
          </div>
        </div>
      </div>
    ))}
  </div>
</section>
