---
/**
 * Página 404 - Página no encontrada
 *
 * Página personalizada para errores 404
 * Ayuda a los usuarios a navegar de vuelta al sitio
 */

export const prerender = true;

import Layout from '../layouts/Layout.astro';
---

<Layout
  title="Página no encontrada - Portal de Empleos Chile"
  description="La página que buscas no existe o ha sido movida"
>
  <div class="min-h-[70vh] flex items-center justify-center px-4 py-12">
    <div class="max-w-2xl mx-auto text-center">
      <!-- Animated 404 Illustration -->
      <div class="mb-8 animate-fade-in">
        <div class="relative inline-block">
          <!-- 404 Number with gradient -->
          <h1 class="text-[120px] md:text-[180px] font-bold leading-none bg-gradient-to-br from-accent via-blue-500 to-purple-600 bg-clip-text text-transparent animate-gradient">
            404
          </h1>

          <!-- Floating elements -->
          <div class="absolute -top-4 -right-4 w-8 h-8 bg-accent/20 dark:bg-accent/30 rounded-full animate-float"></div>
          <div class="absolute -bottom-2 -left-6 w-12 h-12 bg-purple-500/20 dark:bg-purple-500/30 rounded-full animate-float-delayed"></div>
        </div>
      </div>

      <!-- Message -->
      <div class="mb-8 space-y-4 animate-slide-up">
        <h2 class="text-3xl md:text-4xl font-bold text-text-primary dark:text-dark-text-primary">
          ¡Ups! Página no encontrada
        </h2>
        <p class="text-lg text-text-secondary dark:text-dark-text-secondary max-w-md mx-auto">
          Lo sentimos, la página que buscas no existe o ha sido movida.
          Pero no te preocupes, tenemos muchas ofertas laborales esperándote.
        </p>
      </div>

      <!-- Action Buttons -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center mb-12 animate-slide-up-delayed">
        <a
          href="/"
          class="inline-flex items-center justify-center gap-2 px-6 py-3 bg-accent text-white rounded-lg hover:bg-accent-hover transition-all duration-200 font-medium shadow-md hover:shadow-lg transform hover:-translate-y-0.5"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
            class="w-5 h-5"
          >
            <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
            <polyline points="9 22 9 12 15 12 15 22"/>
          </svg>
          Volver al inicio
        </a>

        <a
          href="/"
          class="inline-flex items-center justify-center gap-2 px-6 py-3 bg-white dark:bg-dark-card border-2 border-border dark:border-dark-border text-text-primary dark:text-dark-text-primary rounded-lg hover:bg-surface dark:hover:bg-dark-surface transition-all duration-200 font-medium shadow-sm hover:shadow-md transform hover:-translate-y-0.5"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
            class="w-5 h-5"
          >
            <circle cx="11" cy="11" r="8"/>
            <path d="m21 21-4.35-4.35"/>
          </svg>
          Buscar empleos
        </a>
      </div>

      <!-- Suggestions -->
      <div class="bg-surface dark:bg-dark-surface border border-border dark:border-dark-border rounded-lg p-6 text-left animate-fade-in-delayed">
        <h3 class="text-lg font-semibold text-text-primary dark:text-dark-text-primary mb-4">
          ¿Qué puedes hacer?
        </h3>
        <ul class="space-y-3 text-text-secondary dark:text-dark-text-secondary">
          <li class="flex items-start gap-3">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              class="w-5 h-5 text-accent flex-shrink-0 mt-0.5"
            >
              <polyline points="20 6 9 17 4 12"/>
            </svg>
            <span>Explora las <a href="/" class="text-accent hover:underline">ofertas laborales disponibles</a></span>
          </li>
          <li class="flex items-start gap-3">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              class="w-5 h-5 text-accent flex-shrink-0 mt-0.5"
            >
              <polyline points="20 6 9 17 4 12"/>
            </svg>
            <span>Usa el buscador para encontrar empleos por ubicación o categoría</span>
          </li>
          <li class="flex items-start gap-3">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              class="w-5 h-5 text-accent flex-shrink-0 mt-0.5"
            >
              <polyline points="20 6 9 17 4 12"/>
            </svg>
            <span>¿Eres empleador? <a href="/empleador/registro" class="text-accent hover:underline">Publica una oferta</a></span>
          </li>
        </ul>
      </div>

      <!-- Fun fact or tip -->
      <div class="mt-8 text-sm text-text-secondary dark:text-dark-text-secondary animate-fade-in-more-delayed">
        <p>💡 <strong>Dato curioso:</strong> El error 404 se llama así porque era el número de la habitación en el CERN donde se alojaban los servidores del primer sitio web.</p>
      </div>
    </div>
  </div>
</Layout>

<style>
  /* Gradient animation for 404 number */
  @keyframes gradient {
    0%, 100% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
  }

  .animate-gradient {
    background-size: 200% 200%;
    animation: gradient 3s ease infinite;
  }

  /* Fade in animation */
  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  .animate-fade-in {
    animation: fadeIn 0.6s ease-out;
  }

  .animate-fade-in-delayed {
    animation: fadeIn 0.6s ease-out 0.3s both;
  }

  .animate-fade-in-more-delayed {
    animation: fadeIn 0.6s ease-out 0.6s both;
  }

  /* Slide up animation */
  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-slide-up {
    animation: slideUp 0.6s ease-out 0.2s both;
  }

  .animate-slide-up-delayed {
    animation: slideUp 0.6s ease-out 0.4s both;
  }

  /* Floating animation for decorative elements */
  @keyframes float {
    0%, 100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  .animate-float {
    animation: float 3s ease-in-out infinite;
  }

  .animate-float-delayed {
    animation: float 3s ease-in-out 1.5s infinite;
  }

  /* Responsive adjustments */
  @media (max-width: 768px) {
    h1 {
      font-size: 100px;
    }
  }
</style>
